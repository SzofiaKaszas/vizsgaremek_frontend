import { useContext } from "react";
import { AuthContext } from "../context/authContext";

export function Register(){
    const context = useContext(AuthContext);

    return <div>Register page under construction</div>
}